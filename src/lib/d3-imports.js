import { drag, forceCenter, forceCollide, forceLink, forceManyBody, forceSimulation, line, max, 
    scaleLinear, scaleOrdinal, schemeCategory10, select } from "d3";

export default {
    drag,
    forceCenter,
    forceCollide,
    forceLink,
    forceManyBody,
    forceSimulation,
    line,
    max,
    schemeCategory10,
    scaleLinear,
    scaleOrdinal,
    select
}