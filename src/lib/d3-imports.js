import { forceCenter, forceCollide, forceLink, forceManyBody, forceSimulation, select } from "d3";

export default {
    forceCenter,
    forceCollide,
    forceLink,
    forceManyBody,
    forceSimulation,
    select
}